# Tech Debt Backlog

Tracked technical debt discovered outside the scope of active tasks. Each entry: description, severity (high/medium/low), category, affected modules, estimated effort, and discovering agent. See `@.claude/rules/shared/operational-standards.md` § "Technical Debt Tracking".

| # | Description | Severity | Category | Affected Modules | Est. Effort | Discovered By |
|---|-------------|----------|----------|------------------|-------------|---------------|
| 1 | `/create-pr` still references `/ship-it` as the `--no-push` parent that "owns the Copilot request" (lines ~3, 10, 307, 380). After PR #12, the ship chain reaches `/create-pr`'s push path via `/ship-pr`, so these references are stale. Refresh them so the single-Copilot-requester story reads cleanly. | low | Documentation | `.claude/skills/create-pr/SKILL.md` | ~30 min | code review on PR #12 |
| 2 | `/setup-repo` addresses the shared rules at a **flat** `.claude/rules/{name}.md` path (~12 references: the verification loop that tests `[ -f ".claude/rules/${rule}.md" ]`, and the scaffolding loop that runs `cp {project-template}/.claude/rules/{name}.md .claude/rules/`), and `docs/guides/setup-guide.md`'s directory tree shows the same flat layout. The rules actually live at `.claude/rules/shared/`, and 20+ cross-references across rules and skills use `@.claude/rules/shared/…`. Consequence: the verification loop reports every rule missing, and the scaffolding loop copies from a non-existent source into a destination where the `shared/` references would not resolve in the target project. Make setup-repo's loops, the setup-guide tree, and the scaffolded target layout consistently nested under `shared/`. Deliberately deferred from PR #14 (T-014), which only added two names to the existing loops and did not change the layout. | high | Documentation | `.claude/skills/setup-repo/SKILL.md`, `docs/guides/setup-guide.md` | ~1-2 h | code review on PR #14 |
| 3 | **`markdown` backend only.** Live board state (In Progress, Blocked) does not reach `main` under `board-in-pr.md` — those transitions sit on unmerged branches — but the consumers still read them from the merged `board-context.md`: `/pick-up-task`'s 2-item WIP-limit check, and `/daily-sync`'s In Progress count, WIP violations, blockers, and cycle-time alerts. From a worktree cut from `origin/main` they all see an empty In Progress column and under-report in-flight work. Rework them to derive live state from open PRs and branches (`gh pr list`, `git branch -r`, each branch's own `board-context.md`). Rescoped by T-027: on the `github` backend reads are live API calls and this does not apply, but it still bites every repo on `markdown` — including this one until it is migrated (T-027.8). `board-adapter.md` § Known Limitation and `board-in-pr.md` § What the Committed Board Records document it in the meantime. | medium | Architecture | `.claude/skills/pick-up-task/SKILL.md`, `.claude/skills/daily-sync/SKILL.md`, `.claude/rules/shared/shared-standards.md` (Kanban Protocol) | ~half day | code review on PR #14; rescoped on PR #37 |
| 4 | **`github` backend.** `board.read_task()` resolves a Task ID to an issue number with a `gh issue list --search` call on **every** use, and `/update-board`, `/create-pr` Step 1b and `/address-feedback` Step 8 each call it for the same task. The call count grows with transitions per task, and GitHub's search API has a tighter rate limit than the REST list endpoints. Cache the resolution for the life of a run (the number never changes), or record it once — e.g. in the branch's PR body via `Closes #N` — and read it back. | low | Architecture | `.claude/rules/shared/board-adapter.md`, `.claude/skills/update-board/SKILL.md`, `.claude/skills/create-pr/SKILL.md`, `.claude/skills/address-feedback/SKILL.md` | ~2 h | code review on PR #37 |
| 5 | The T-027 board row carries no acceptance criteria and does not link them: `agent-preamble.md` start step 5 requires clear acceptance criteria before work begins, and the RFC's story table lists scope, not Given/When/Then. Add acceptance criteria for T-027 and its stories (the RFC is the natural home) and link them from the board row, so the end-of-task walkthrough has something to walk. | low | Documentation | `board-context.md`, `docs/artifacts/rfc/T-027-RFC-GitHub Issues Board Backend.md` | ~1 h | code review on PR #37 |
| 6 | `/setup-repo` 6e's markdown scaffold template still generates `## Backlog`, `## Done (recent)` and `## Decisions Log` sections inside `board-context.md`, contradicting the board split in `board-adapter.md` (those live in `docs/board/`, which the same step also creates). A freshly scaffolded board therefore fails `parse_board.py --check` on the Done and Decisions Log sections before anyone has used it. Trim the template to the four live columns. | medium | Documentation | `.claude/skills/setup-repo/SKILL.md` | ~30 min | address-feedback pass on PR #37 |
| 7 | **Bundled-script cache fallback ignores the installed version.** `/migrate-board` Step 1 and `hooks/pre-push` (for `sync-rule/mirror.sh`) fall back to the *newest* copy in `~/.claude/plugins/cache`, not the version installed for the project, so the script can come from a different plugin version than the running skill. `sort -V` also ranks a SHA-named version folder above `1.2.10`, and `*/tech-agency/*` matches other marketplaces (e.g. `tech-agency-dev`). Prefer the matching `installPath` from `~/.claude/plugins/installed_plugins.json`, keep newest-cached as last resort, and test an installed version older than the newest cached one. | medium | Architecture | `.claude/skills/migrate-board/SKILL.md`, `hooks/pre-push` | ~half day | code review on PR #39 |
| 8 | **`/migrate-board` does not guard an empty `$PARSER`.** An agent in a fresh shell that skips Step 1's resolve block gets a vague `python3 ""` error. Use `python3 "${PARSER:?run the resolve-parser block in Step 1 first}"` and widen the guard test to accept that form. | low | Code Quality | `.claude/skills/migrate-board/SKILL.md`, `test_parse_board.py` | ~30 min | code review on PR #39 |
| 9 | **`debt_check` does not apply "a live task is never frozen".** `parse_board.py` `debt_check` ("active debt but its board task is Done") builds its Done set from `done_task_ids()` without removing live IDs, unlike `board_tasks`. So a task that is live *and* has an earlier phase in Done (`TD-337` in Ready, `TD-337 (phase 1)` in Done) blocks `--tech-debt --done freeze`, and the only way past it is editing a closed quarter. Subtract live IDs, as `board_tasks` does, and add the test. Check whether Steady's TD-028 / TD-056 problems are this case before resolving them by hand. | medium | Code Quality | `.claude/skills/migrate-board/parse_board.py`, `test_parse_board.py` | ~1 h | re-review on PR #40 |
| 10 | **`check_push_safety.py` gaps with no current instance in the repo.** Not flagged: wildcard refspecs (`'refs/heads/*:refs/heads/*'`, which writes main like `--all`); upstream set later (`git branch -u origin/main`, `--set-upstream-to`); combined short flags (`checkout -tb`); `switch --create=name`; `git branch -c main feature` copying main's upstream; commands split with `\` line continuations; `"v$X:$TARGET"`, where the tag shortcut runs on the whole refspec and skips the destination check; `/usr/bin/git` or `"$GIT"` invocations. Also: under freeze, `verify` undercounts a live task repeated twice in Done (it still fails). Close these with tests so the checker holds as skills grow. | low | Testing Gaps | `scripts/check_push_safety.py`, `scripts/test_check_push_safety.py`, `.claude/skills/migrate-board/parse_board.py` | ~half day | re-review on PR #40 |
| 11 | **`/migrate-board` bare-word agent matching still mislabels some prose.** With no agent @mention in an Agent cell, `agent_names` counts any capitalised agent name, so "Swift UI port" gets `agent:@Swift`, "Link to PR #12" gets `@Link` and "Pipeline owner: TBD" gets `@Pipeline`. `--check` does not report these, because a label was produced. A wrong agent label counts toward that agent's 2-item WIP limit. Options: only accept bare names when the whole cell is agent names and separators, or have `--check` list every bare-word match for confirmation. | low | Code Quality | `.claude/skills/migrate-board/parse_board.py` (`agent_names`), `SKILL.md` Step 4 | ~1 h | fourth review on PR #41 |

## Stranded consumer improvements — not yet promoted (T-016.6, 2026-09-01)

Found while building the rules mirror: the reference consumer had rule content the plugin never received. These four are genuinely generic and should be promoted, but each needs careful genericization (they carry concrete module paths and project ticket IDs) and so were left out of T-016.6, which took only the corrections and the hooks.

| Item | Rule | Severity | Why it is generic |
|---|---|---|---|
| Presentation module split (`sharedPresentation` + `list`/`detail`/`form` per feature) | `mobile/shared/kmp-coding-standards.md` | Medium | A layering rule about surface modules, independent of any one feature |
| Pipeline Store for retained observation pipelines | `mobile/shared/kmp-coding-standards.md` | Medium | Keeps pipeline keys out of `State`; applies to any MVI screen with a long-lived `combine` |
| `*StateFactory.kt` file-organization suffix | `mobile/android/compose-coding-standards.md` | Low | Distinguishes construction-with-UI-decisions from pure translation (`*Mapper`) |
| Per-changed-module test running in pre-push (replacing `assemble`) | `shared/git-hooks.md` | Medium | `assemble` triggers the OOM-prone release/R8 build; per-module tests cover compilation more cheaply |

Discovered by: @Claude. The consumer keeps these in `.claude/rules-local/` until promoted.
